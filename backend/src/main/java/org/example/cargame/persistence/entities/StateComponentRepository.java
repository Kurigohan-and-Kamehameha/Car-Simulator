package org.example.cargame.persistence.entities;

import org.springframework.data.jpa.repository.JpaRepository;

public interface StateComponentRepository extends JpaRepository<StateComponentEntity, Long> {
}
