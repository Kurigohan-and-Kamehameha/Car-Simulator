package org.example.cargame.persistence.entities;

import org.springframework.data.jpa.repository.JpaRepository;

public interface SpeedComponentRepository extends JpaRepository<SpeedComponentEntity, Long> {
}
