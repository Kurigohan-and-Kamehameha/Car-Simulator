package org.example.cargame.persistence.entities;

import org.springframework.data.jpa.repository.JpaRepository;

public interface ColorComponentRepository extends JpaRepository<ColorComponentEntity, Long> {
}
