package org.example.cargame.persistence.entities;

import org.springframework.data.jpa.repository.JpaRepository;

public interface PathComponentRepository extends JpaRepository<PathComponentEntity, Long> {
}
